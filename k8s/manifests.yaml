---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: argocd-status-checker
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-status-checker
    app.kubernetes.io/component: monitoring

---
# Read-only access to Application CRs — nothing else
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: argocd-status-checker
  labels:
    app.kubernetes.io/name: argocd-status-checker
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["applications"]
    verbs: ["get", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: argocd-status-checker
  labels:
    app.kubernetes.io/name: argocd-status-checker
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: argocd-status-checker
subjects:
  - kind: ServiceAccount
    name: argocd-status-checker
    namespace: argocd

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-status-checker
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-status-checker
    app.kubernetes.io/component: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-status-checker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: argocd-status-checker
    spec:
      serviceAccountName: argocd-status-checker
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: checker
          # Build and push to your registry, then update this:
          image: ghcr.io/dorbeus/argocd-argocd-status-checker:main
          pullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: PORT
              value: "8080"
            - name: ARGOCD_NAMESPACE
              value: "argocd"
            # Optional: scope to specific apps by label
            # - name: LABEL_SELECTOR
            #   value: "app.kubernetes.io/instance=my-appset"
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]

---
apiVersion: v1
kind: Service
metadata:
  name: argocd-status-checker
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-status-checker
    app.kubernetes.io/component: monitoring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: argocd-status-checker
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
